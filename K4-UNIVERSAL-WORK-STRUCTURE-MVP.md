# K4 通用工作结构与审计方法

> 文档性质：保存性工程 MVP。
>
> 本文把一个对象的有限可见表示、静态总账、实际变化历史与五项工作责任组织为可复核的
> 通用结构。静态闭包、动态规范形、条件最小性与高维保真边界的唯一数学权威是
> [《K4 的静态闭包与动态规范形》](./K4-MATHEMATICAL-STRUCTURE-CANDIDATE.md)。本文只声明
> 如何解释和使用那些构造，不重新定义或证明它们。
>
> 本文不是具体产品、组织、运行环境或权限系统的实现合同，也不证明任意对象天然适合
> 四维表示。它保留来源复合文档中的工程语义，为后续独立完善提供可读起点。

## 0. 使用边界

本文直接处理的是对象的冻结表示和实际证据，不是对象不可见真实状态的全知副本。结构
填写完整、形式计数吻合、过程走完、单次运行成功或表达流畅，都不能自行抬升主张强度。

数学文章给出两个彼此独立的候选基：静态观察函数基 `S` 与动态变化关系基 `D`。具体应用
必须分别提供状态域、问题类、变化域、不可约见证、共同覆盖证明和解释证据。相同数量不
产生 `S` 与 `D` 的配对，静态位置不预装动态动作。

四个 rank-3 静态位置是否应普遍解释为“从静态到动态的转换面”仍是未决假设。本文不
采用、不扩展也不否定该假设；在另有解释证据以前，rank-3 只表示三个静态面共同读取时的
局部汇合位置。

## 1. 对象、表示与判断上限

### 1.1 冻结对象

目标 `X` 是报告最终谈论的对象。冻结表示 `R_v` 是本次能够读取的有限来源及其内容身份：

```text
R_v={(r_1,id_1),…,(r_n,id_n)}.
```

表示关系 `ρ(R_v,X)` 说明这些来源在声明范围内表示目标，但始终保持：

```text
R_v ≠ X.
```

来源声称 `P(X)` 与目标实际满足 `P` 是两种主张。文档、实现、运行现场和样本可以互相
偏离；发现偏离是结果，不是删除表示关系。

观察尺度为 `σ=(u,b,g)`：`u` 固定唯一主语，`b` 固定纳入与排除边界，`g` 固定最小辨认
粒度。证据截止点 `τ` 冻结本次允许纳入的来源版本。一次冻结对象为：

```text
O=(X-ref,R_v,σ,τ).
```

目标指称、来源成员或版本、尺度任一分量或截止点变化，都会形成新对象；不能把新旧判断
混成一份无尺度报告。

### 1.2 证据、方法与报告

`E_O` 是在 `O` 范围内实际进入判断的有限证据快照。可引用但未读取的材料不属于该快照。
方法 `M` 是公开的有限判断规则：

```text
M:(O,E_O) ↦ Q.
```

一个最小报告条目保存对象、位置、认识类型、陈述、证据与规则、当前状态及主张上限。
认识类型至少区分已验证事实、带来源陈述、推断和未知。报告可以索引五类问题：覆盖、接缝、
一致性、变化与未知；这些是问题索引，不是新的本体维度。

所有报告必须满足：

```text
Claims(Q) ⊆ Cn(E_O,M).
```

若两个可能世界向 `M` 提供完全相同的输入，而目标命题在两个世界真假不同，任何只读这些
输入的方法都不能保证判定该命题。诚实结果只能保留来源陈述、条件结论或未知。因此位置
填满不是目标真实性或正确性的充分条件。

### 1.3 四种质量判断

以下四问分别判断：

1. **结构完备**：声明的必要位置和关系都有明确状态；
2. **证据完备**：每个非未知主张都有可回源证据，推断规则和限制可见；
3. **运行正确**：在声明环境、输入域和窗口内，实际执行满足合同；
4. **价值正确**：结果符合有权主体采用的目的、权利边界和取舍尺度。

四者可以互相提供证据，但任一单项都不推出“对象整体正确”。同一来源在文档尺度可得到
结构完备结论，在产品尺度却可能因缺少运行证据而只能保持未知。

## 2. 领域解释

具体领域分别建立偏映射：

```text
φ_S:L_S ⇀ S,
φ_D:L_D ⇀ D.
```

偏映射允许证据不足的术语保持未映射。每个赋值必须保存术语、基元素、可观察谓词、范围、
来源、正见证、与其他候选的区分依据、反证条件和状态。静态解释必须联系分离或覆盖责任；
动态解释必须联系不可模拟变化、模拟边界或覆盖责任。

裸基被重命名时，解释映射必须一同复合。只重命名基而固定映射不动，得到的是不同解释。
名称熟悉、下标相同或一个案例吻合，都不能建立身份。

## 3. 十六位静态总账

### 3.1 坐标与导航

引用数学文章的 `B_4=P(S)`，每个 `I⊆S` 对应唯一位置 `L_I`：

```text
rank 0: L_empty
rank 1: L_1, L_2, L_3, L_4
rank 2: L_12, L_13, L_14, L_23, L_24, L_34
rank 3: L_123, L_124, L_134, L_234
rank 4: L_1234
```

包含、meet、join 和补只导航坐标：

```text
L_I≤L_J ⇔ I⊆J,
L_I∧L_J=L_(I∩J),
L_I∨L_J=L_(I∪J),
¬L_I=L_(S\I).
```

四维格有 32 条直接覆盖边，每个位置忽略方向后有四个邻位。坐标可达不表示内容成立；
join 不自动合并文本，补不表示语义相反，邻接不产生因果或工作顺序。

### 3.2 位置记录与五种状态

每个位置至少保存：坐标、唯一主问句、状态、主张引用、证据引用、适用范围，以及不适用时
的边界理由。状态只取：

- `sourced`：已有可回源回答，当前范围内未见未决冲突；不自动表示真实或通过；
- `gap`：责任适用，但没有承担回答的内容或必要引用；
- `conflict`：同一范围内存在尚未裁决的不相容来源主张；
- `unknown`：已有材料但当前不足以回答，或关键证据可得性未知；
- `not-applicable`：有可复核边界理由证明问题不适用。

没有材料不是不适用，未知不是否定。若证据足以回答“没有额外约束”，该否定回答仍可为
`sourced`。

### 3.3 十六个主问句

`L_empty` 固定对象、表示、版本、尺度、截止点和边界。

四个单面 `L_i` 分别问：`s_i` 独立承担什么可观察责任，其正见证、分离见证、范围与来源
是什么？一个面的回答不能替另一个面。

六个二元位置 `L_ij` 分别问：同时读取 `s_i(x)` 与 `s_j(x)` 时，两项结果在共享范围内
必须满足什么兼容条件，当前支持、冲突和未知是什么？两个单面都有内容不替代接缝证据。

四个三元位置 `L_ijk` 分别问：三项共同读取时，哪些汇合条件不能由三条两两关系分别成立
来替代，缺少的第四面怎样限制结论？三个二元位置分别无冲突，不证明三项合取成立。

`L_1234` 问：在同一对象、版本、尺度与截止点下，前十五个位置能否共同支持一个有限、
一致且保留缺口的当前综合结论？综合位置不能覆盖下位冲突、未知或只在四面同时出现时才
可见的约束。

结构关系不传播状态、主张或证据。每一位置都需要直接回答其主问句的依据。

### 3.4 唯一权威

语义单元是能独立承担来源、范围、版本和变更责任的最小内容。每个当前稳定单元只有一个
权威引用。一个单元可以被多个位置使用；增加的是位置到同一单元的引用，不是权威副本。

若解释改变谓词、范围、结论、义务、例外或效力，就形成新的派生单元，并保留来源关系。
仅缩小使用窗口、改变排版或建立索引不改变单元。复制正文却丢失来源身份时，副本只能标为
未解析或冲突，不能凭相似性猜测权威。

四面体图像只能帮助记忆 `4—6—4—1` 与补对。删除图像后，总账定义和问句仍完整；所以
图像不能提供本体、证据、权限、因果或执行顺序。

## 4. 实际历史与动态投影

### 4.1 静态—动态接缝

时点 `t` 的静态账户 `A_t` 是依据静态解释形成的观察、证据、冲突与未知，不是世界状态
本身。从该截止点到下一截止点的实际历史 `H_t` 由真实发生并被记录的事件组成。每个事件
至少保存身份、可支持的方向解释、前后状态或结果、成功/失败/部分生效/未知，以及证据。

只有同时满足尺度相同或有尺度迁移证据、历史落在两个截止点之间、变化可回到事件来源、
后继账户重新观察、失败与部分效果未被删除时，才写：

```text
A_t ==[H_t]=> A_(t+1).
```

方向帮助解释历史，不把意图变成事件，也不允许由预计结果计算后继账户。

### 4.2 occurrence、嵌套与并发

数学文章定义了 64 个有限有根形式、条件栈解析与并发偏序。工程使用还必须满足：

1. 相同形式的两次发生使用不同事件身份；
2. occurrence 保留完整事件区间，不能删除不符合预期的中间事件；
3. 打开与恢复属于同一责任必须有证据，重复标签本身不构成返回；
4. 失败、部分生效和未知不由形式名称改写；
5. 配对缺失、区间交叉或并发不可比时，保留候选、冲突或未知；
6. 并发历史保留偏序、分支和汇合证据，显示顺序不制造因果。

动态分类投影可以保存已识别 occurrence、候选、冲突、未知和未匹配事件。它必须回指原始
历史，可以重新生成但不能覆盖、截断或重排历史。`64` 与 `16×4` 的数值相等不是语义
构造；静态总账没有“每格四个固定动作”。

## 5. 五项工作责任

一次有界尝试记录为：

```text
C_t=(A_t,G_t,P_t,H_t,Z_t,Δ_t).
```

其中账户、目标、计划、历史、结算和静态差分候选各自承担不同责任。`Δ_t` 是结算输出，
不是第六个阶段，也不是新账户。

### 5.1 Observe

输入是可辨认来源、观察尺度、截止点和可选前账户；输出是静态账户 `A_t`。它保持对象、
尺度、来源和认识状态可辨认，不把推断、偏好或行动意图写成观察事实。它可以登记机会、
缺口、冲突与未知，但不选择本轮变化、规定路线、执行效果或结算尚未发生的过程。

### 5.2 Goal

Goal 从 `A_t` 选择并冻结一次变化责任 `G_t`：直接变化对象、所有已知关联影响、保持冻结
的范围、可观察验收、结果归属和动态预算。它不选择具体操作图，也不宣称结果已发生。

若只改变一个静态面 `s_i`，且四维解释已取得证据、对象身份与尺度不变，则最大关联域为：

```text
N_i={J∈B_4 | s_i∈J, 1≤|J|≤3}.
```

它含一个单面、三条接缝和三个三元汇合，共七位。空位置冻结，全集只允许声明预期观察；
其余不含 `s_i` 的七位构成冻结补域。多单面直接变化、整体替换或尺度变化不能藏入该特例。

动态预算：

```text
B_G=(allowed_directions,maximum_effects,resource_bounds,
     depth_bound,concurrency_bound,stop_conditions).
```

静态关联域与动态预算类型不同，任一方都不能唯一推出另一方。

### 5.3 Plan

Plan 把 Goal 允许空间剪成有限有向无环图 `P_t`。每个真实节点声明操作、预期动态方向、
输入、前置检查、最大副作用，以及局部 `pass/fail` 的冻结后继；无副作用的分叉与汇合可以
作为结构节点。Plan 必须保持在 Goal 的资源与效果边界内，但不执行节点、记录实际结果或
修改 Goal。

同一含分支的 Plan 可以许可多条历史，所以计划存在、可达或预计通过都不是发生证据。
运行时出现图中没有定义且不能由终止规则处理的事实，只能停止、失败或保留未知；现场改图
得到的是新 Plan。

### 5.4 Run

Run 读取冻结 Goal、Plan 与实际条件，执行被激活节点并追加实际历史 `H_t`。每个事件保存
输入、输出、局部结果、副作用与证据，局部结果只选择冻结后继，不判断整个 Goal。

历史只追加；后来的成功不能删除首次失败。Run 不修改 Goal、不重画 Plan、不补写未发生
节点，也不在图外扩大副作用。若父节点调用必须独立复核的子过程，父历史保存调用、子身份
与尺度、子历史引用、返回和恢复证据；它既不复制全部子事件，也不把子过程压成无来源的
“成功”。被当前尺度明确视作原子的确定性调用只需保存其输入、输出、效果与证据。

### 5.5 Finish

Finish 读取 `A_t,G_t,P_t,H_t` 与实际结果面，输出结算 `Z_t` 和最小静态差分候选 `Δ_t`。
它逐项判断 Goal 的验收与守恒条件，列出执行、未执行、失败、未知和残余效果，比较计划与
历史，保存动态投影、结果归属、未完成内容和恢复位置。

结算摘要可以由多条历史映到同一短文本，但必须保留历史引用、被省略信息类型和主张上限。
只保存最终 `success` 无法区分“一次通过”与“先失败产生效果后重试成功”，所以不能替代
历史。

`Δ_t` 每项保存位置、前项引用、候选陈述与状态、证据、变化理由和主张上限。它只供下一次
Observe 重新判断，不能直接写成新账户。Finish 不补做操作、不修改 Goal 或 Plan、不删除
历史，也不因结算取得采用权。

### 5.6 责任总表

| 责任 | 输入 | 输出 | 不可替代的职责 |
| --- | --- | --- | --- |
| Observe | 来源、尺度、截止点 | `A_t` | 形成有来源的当前静态账户 |
| Goal | `A_t` | `G_t` | 冻结值得承担的变化、影响、验收与预算 |
| Plan | `G_t` | `P_t` | 冻结有限或然路线 |
| Run | `G_t,P_t` 与实际条件 | `H_t` | 记录实际发生及局部路由 |
| Finish | `A_t,G_t,P_t,H_t` 与结果 | `Z_t,Δ_t` | 有界结算并交出后继观察输入 |

依赖只产生读取接缝，不产生上下级、权限、正确性或反向修改权。五项责任也不要求五个固定
进程、目录或执行主体。

## 6. 理论校准来源

五项责任接受不同学科的有限校准：

| 责任 | 主锚点 | 条件性辅助 | 不能单独证明 |
| --- | --- | --- | --- |
| Observe | 形式认识论 | 信息论；有经验测量时的统计与测量理论 | 来源真实、账户等于世界 |
| Goal | 规范决策理论 | 风险、伦理；存在战略主体时的博弈论 | 目标可达、行动发生、唯一价值规则 |
| Plan | 运筹学 | 图论、调度、形式方法 | DAG 可执行、资源可行、最优或实际轨迹 |
| Run | 控制理论 | 状态估计、约束控制、并发过程 | 稳定、收敛、鲁棒或整个 Goal 通过 |
| Finish | 审计理论 | 结算、信息论；提出因果问题时的因果推断 | 摘要无损、先后即因果、结算即采用 |

学科是否适用取决于本次实际对象和主张：没有战略主体就不启用博弈解释，没有时间与竞争
资源就不宣称解决调度，没有因果问题就不要求反事实归因。外部理论限制本文可说什么，但不
替本文生成事实、授权或发布结果。

## 7. 三种审计方法

一次应用先冻结：对象 `O`、Baseline `B`、静态解释 `I_S`、动态解释 `I_D`、五责任记录、
证据快照和方法版本。中途改变任一项就形成新应用。

### 7.1 静态审计

静态程序依次：冻结身份；预检四面解释；机械生成 `1—4—6—4—1`；逐位取证；检查六个
接缝、四个三元汇合、身份与整体；检查来源和唯一权威；生成保留全部 16 项状态与上限的
压缩报告。冻结输入有限时，该程序有限停止。

### 7.2 动态审计

动态程序只从实际事件出发：冻结历史；逐事件取证；按证据建立全序或偏序；与 Goal 预算
和 Plan 路线对读；只在有配对证据时解析嵌套；在不删除事件的前提下识别有限形式；输出
动态投影、Finding 候选与未知。Plan、标签形状和最终成功都不能制造事件。

### 7.3 责任审计

责任程序冻结 `A_t,G_t,P_t,H_t,Z_t,Δ_t` 的实际存在项，逐项检查输入、输出、不变量与
禁止项，再检查四个主要接缝和反向改写。它只形成报告，不重演工作，也不取得修改前项的
权力。

### 7.4 十类 Finding

固定最小类型为：静态空缺、接缝断裂、三方冲突、整体过强、来源不足、动态越界、时序未知、
括号无证据、压缩丢失、尺度漂移。每项必须绑定对象、位置、Baseline、陈述、认识类型、
证据、反证条件、状态、处置和主张上限；条件未达到时只能保存候选问题或未知。

未发现 Finding 只表示在冻结方法、范围和证据中没有发现这些偏离，不证明对象没有其他
缺陷。“四缺一”也只有在一个独立来源已预先定义四项身份、责任、共同闭合规则与适用证据
时，才能报告相对空缺。只有三个观察项时，区分见证最多证明当前表示不足，不能制造或命名
唯一第四项。

## 8. 可证伪性与停止边界

每项准备承担解释或方法责任的核心主张，应冻结：主张身份、类型、陈述、前提、范围、依赖、
反证、有限测试、证据截止点、结果规则和主张上限。测试结果只取：

- `survived`：完整测试未发现该类反证，不构成全域证明；
- `narrowed`：证据否定原范围的一部分，但支持明确真子主张；
- `refuted`：有效见证满足前提却否定结论；
- `unknown`：输入、方法或证据不足，无法落入前三类。

五个核心反证入口分别针对：`R_4` 条件最小性；双基分立与无典范配对；64 计数；高维无损
约减的结构相容；可复核静态更新对实际历史来源的需要。反例必须满足原主张全部前提；删去
前提后的相似案例只能支持缩小边界或提出新主张。

一次有限验证采用单向依赖：冻结对象与已知债务；机械清点；全文一致性审计；执行五个反证
入口；独立对象应用；独立复核；若另有授权则至多形成一个修正 Candidate；对新身份至多
回归一次；最后由有权主体决定是否采用。任何修正都产生新内容身份，旧结果不能自动继承。

本文只区分四种状态：

1. `body-complete`：正文责任存在；不表示一致或正确；
2. `audit-ready`：对象身份、来源、债务、方法和反证入口已冻结；
3. `validation-survived`：同一身份完成冻结验证且没有未处置的有效反驳或阻断性未知；
4. `adopted`：有权主体批准，并由另行授权的发布使其成为 Current。

状态只能由绑定精确内容身份的外部证据取得。正文不能自证已经通过验证或取得采用。

## 9. 当前 MVP 的准确位置

本文完成的是从复合来源中分离通用工程语义的保存性 Candidate：它保留对象与证据边界、
16 位静态总账、实际历史、五项责任、学科校准、三种审计方法、Finding 与可证伪结构；
数学证明统一引用独立文章。

它尚未验证四个静态面或四个动态方向对任一领域是否充分，尚未解决 rank-3 转换面假设，
也没有进入任何具体特化或实现。因此本文件的存在最多证明保存性正文已形成，不能自行取得
`audit-ready`、`validation-survived` 或 `adopted`。下一轮可以在不改数学权威的前提下，
把本文作为独立对象审计和完善。
