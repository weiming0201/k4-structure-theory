# K4 的静态闭包与动态规范形

> 文档性质：近纯数学 Candidate。
>
> 本文定义两类彼此独立的有限结构，证明它们从一维到四维的计数、分辨率条件的首个
> 满足维数和高维分解边界。本文不主张任意现实对象天然具有四个维度，也不为这些维度
> 指定领域名称。
> 任何领域解释、取证方法、工作过程和实现合同均属于另一篇文章。

## 0. 主张范围

本文只使用四种陈述：定义、由定义推出的结论、明示前提下的条件命题，以及反例所限定的
适用边界。计数相等不产生类型等同；存在一种编码不产生同构；局部四分不产生全局充分性。

设可能状态域为 `U`，待区分的问题类为 `Q`。对动态结构，设状态域为 `X_D`，允许变化关系
为 `T ⊆ X_D×X_D`，状态等价关系为 `≈`，允许中间状态与有限路径长度上界为 `L`。所有不可约、
覆盖和模拟结论都相对于这些已声明对象成立；改变它们就改变了命题。

## 1. 两个独立的基

### 1.1 静态基

一个静态观察面是函数：

```text
s_i : U → V_i
```

令 `S={s_1,…,s_n}`，并记联合观察为：

```text
F_S(x)=(s_1(x),…,s_n(x)).
```

下标只用于寻址，不赋予顺序、重要性或动态含义。

`s_i` 相对于 `U` 不可由其余观察面重建，是指存在 `x,y∈U`，满足：

```text
∀j≠i, s_j(x)=s_j(y),     但 s_i(x)≠s_i(y).
```

这一对 `(x,y)` 是分离见证。没有找到见证不等于已经证明可约；可约要求给出重建函数
`r_i`，并在整个声明域上证明：

```text
s_i = r_i ∘ F_(S\{s_i}).
```

`S` 共同覆盖问题类 `Q`，是指对每个 `q∈Q` 都存在公开解码函数 `c_q`，使：

```text
q = c_q ∘ F_S.
```

当 `n=4`、每个面都有不可约见证且 `S` 共同覆盖 `Q` 时，称 `S` 是 `(U,Q)` 上的静态
四维基。该名称只记录一个相对结构，不断言四是任意问题的最小数。

### 1.2 动态基

一个动态方向 `d_i` 是 `T` 的一个子关系。令：

```text
D={d_1,…,d_n},     d_i⊆T.
```

给定 `P⊆D`，变化 `(a,b)` 可被 `P` 有界模拟，是指存在长度不超过 `L` 的路径：

```text
a=a_0 → a_1 → … → a_m,     0≤m≤L,
```

每条边都属于 `P` 中某个关系，全部中间状态位于声明域，并且 `a_m≈b`。

`d_i` 动态不可约，是指 `d_i` 中至少存在一个变化，不能由 `D\{d_i}` 在上述边界内模拟。
动态共同覆盖则要求 `T` 中每个变化都能由 `D` 的有限路径模拟。二者都依赖 `T、≈、L`
及中间状态边界；尚未构造出模拟路径只产生未知，不构成不可约证明。

当 `n=4`、四个方向各有不可约见证且共同覆盖 `T` 时，称 `D` 是相对于声明边界的动态
四维基。方向是变化关系的类型，不是一次已经发生的事件。

### 1.3 独立对称与无典范配对

只保留上述不可约和覆盖关系的 `S` 与 `D` 称为裸基。任意
`π_S∈Sym(S)` 与 `π_D∈Sym(D)` 可以独立重命名两个裸基；定义及见证同步重命名后，真假
不变。

**命题 1（独立标签置换不变性）。** 只由裸基定义推出的命题，在
`Sym(S)×Sym(D)` 的独立作用下不变。

**证明。** 定义只量化某元素能否由其余元素重建或模拟，以及全集能否覆盖声明域。
一致重命名把分离见证、解码器、模拟路径和覆盖证明双射到重命名后的结构。证毕。

**命题 2（无典范配对）。** 仅由 `|S|=|D|=4` 不能推出 `S→D` 的典范双射。

**证明。** 两个四元素集合之间有 `4!` 个双射。若某个 `f:S→D` 由裸结构唯一确定，它应
与任意独立置换相容。固定 `S` 而交换 `D` 中两个元素，就会要求 `f` 的满像同时被该交换
逐点固定，矛盾。证毕。

因此 `s_i=d_i` 在类型上没有定义，相同下标也不是联系证据。具体领域可以另加两个偏解释
映射，但那是裸基以外的结构。

## 2. 从一维到四维

### 2.1 静态布尔闭包

对正整数 `n`，令：

```text
E_n={e_1,…,e_n},     B_n=P(E_n).
```

`B_n` 中元素是无序子集，秩为子集基数。第 `k` 秩有 `C(n,k)` 个元素，故：

```text
|B_n| = Σ_(k=0)^n C(n,k) = 2^n.
```

从一维到四维的秩向量与总数为：

| n | 秩向量 | 总数 |
| --- | --- | --- |
| 1 | `(1,1)` | 2 |
| 2 | `(1,2,1)` | 4 |
| 3 | `(1,3,3,1)` | 8 |
| 4 | `(1,4,6,4,1)` | 16 |

四维闭包有一个空位置、四个单元位置、六个二元位置、四个三元位置和一个全集位置。在
`n=4` 时，每个三元位置严格包含于唯一的四元位置，且该四元位置恰是全集。`n<3` 时不存在
三元位置，`n=3` 时不存在四元位置；`n>4` 时每个三元位置包含于多个四元位置，且没有四元
位置是全集。

完整布尔闭包是一项前提：它保留任意子集。如果对象要求顺序、允许重复、禁止部分组合，
或组合关系不是集合包含，就必须更换结构。

### 2.2 动态有限规范形

令 `D_n={d_1,…,d_n}`。定义长度为 `k` 的互异有序词集：

```text
W_n(k)={d_(i_1)…d_(i_k) | i_1,…,i_k 两两不同},
W_n=⋃_(k=1)^n W_n(k).
```

于是：

```text
|W_n(k)|=P(n,k)=n!/(n-k)!,
|W_n|=Σ_(k=1)^n P(n,k).
```

从一维到四维的长度向量与总数为：

| n | 长度向量 | 总数 |
| --- | --- | --- |
| 1 | `(1)` | 1 |
| 2 | `(2,2)` | 4 |
| 3 | `(3,6,6)` | 15 |
| 4 | `(4,12,24,24)` | 64 |

`B_n` 的元素是无序子集，`W_n` 的元素是有序词。`B_2` 与 `W_2` 虽同为四个元素，也不
产生逐项对应。静态空集与动态空词 `ε` 不同；`W_n` 不计 `ε`，不表示恒等变化不存在。
静态全集只有一个，而四维满长词有 `4!=24` 个。

### 2.3 `R_4` 的首个满足维数

定义分辨率条件 `R_4`：表示必须是某个完整 `B_n`；秩 1、2、3、4 均非空且保持不同；
至少存在一个三元位置严格包含于一个四元位置；并要求同时辨认单项、两两、三项局部汇合
与四项汇合。在 `n=4` 时，四项汇合同时是全集。

**登记结果（来源定理 1）。** 满足 `R_4` 的 `B_n` 中，首个可能的 `n` 是 4。

**证明。** `n=1,2,3` 的最高秩分别为 1、2、3，不能同时保留秩 1 至 4。`n=4` 的这些
秩均非空，每个三元子集严格包含于唯一四元全集。因此 4 首次满足全部条件。证毕。

该结果直接来自 `R_4` 要求秩 1 至 4 均非空，不是独立的普遍最小性定理，也不说明为何应
选择 `R_4`。以下五个反例给出边界：

1. 只区分一个二元性质时，`B_1` 已充分；
2. 只有三个独立方面且三项汇合就是整体时，`B_3` 可充分；
3. 结构带顺序、重复或禁止组合时，`B_n` 可能不适用；
4. 实际序列可以重复同一方向，因而不属于任何互异词集；
5. 若确有第五个不可约静态面或动态方向，把它压入四维会丢失见证。

维数由要保留的差异决定，不由数字、图像或一次成功解释决定。

## 3. 有根形式、历史与偏序

### 3.1 有根简单回返形

对 `w=d_(i_1)…d_(i_k)∈W_4(k)`，定义：

```text
root(w)=d_(i_1),
Loop(w)=(root(w),w,root(w)).
```

其线性显示为在 `w` 后附一个返回根标记。该标记只是显示约定，不是额外发生的事件，也不
属于核心词或增加阶数。以 `α、β、γ、δ` 表示不同动态方向时，一至四阶分别可显示为
`αα、αβα、αβγα、αβγδα`。两个有根形式相同，当且仅当核心词逐位相同。

**命题 3（64 个有根形式）。** 四方向上的全部非空有根简单回返形按阶为
`4,12,24,24`，合计 64。

**证明。** `w↦Loop(w)` 是 `W_4` 到有根形式集的双射，返回标记由首字母唯一决定，不
增加选择。证毕。

这不是唯一可选的身份规则。三种规则必须分开：

| 规则 | 各阶计数 | 总数 | 身份约定 |
| --- | --- | --- | --- |
| `Rooted` | `4,12,24,24` | 64 | 所有阶保根、保序、保反向 |
| `Mixed` | `4,12,8,24` | 48 | 三阶商掉起点，其他阶采用不同规则 |
| `Cyclic` | `4,6,8,6` | 24 | 每一阶统一商掉循环旋转 |

二阶 `AB/BA`、三阶 `ABC/BCA` 和四阶开放路径的身份在三种规则间并不一致，所以不能在
不改变等价关系时混用其计数。

### 3.2 有限形式不是全部历史

一个顺序历史是有限事件序列 `H=(η_1,…,η_m)`，每个事件有独立身份；方向标签可以重复。
一个形式在历史中的 occurrence 是一个闭事件区间及其端点配对，使该区间的方向标签在
给定身份规则下实现该形式。形式可以一次也不发生，也可以发生多次。

**命题 4（历史长度无统一有限上界）。** 若至少一个方向允许任意有限次重复，则可能历史
的长度没有统一有限上界，64 个形式不能枚举全部历史。

**证明。** 对任意 `m>0`，取含 `m` 个不同事件身份、方向标签均为 `d` 的历史 `H_m`。
若存在统一上界 `N`，取 `m=N+1` 即矛盾。证毕。

因此形式集合是有限分类语法，不是发生事实的替代物。

### 3.3 条件栈解析

在全序历史中，若端点配对已经给定，且任意两个闭区间要么不交、要么一者完整包含另一者，
则配对良构嵌套。按事件顺序压栈、在遇到与栈顶相配对的端点时出栈，可形成嵌套树。收缩
一个已闭合子区间得到派生历史；派生历史与原历史是不同的数学对象。

**命题 5（栈解析的条件唯一性）。** 端点配对唯一且良构时，或固定的确定性匹配规则对
每个标签串产生唯一良构配对时，栈解析相对于已给输入产生唯一嵌套树；既无配对也无固定
规则时，解析一般不唯一。

**证明。** 在给定配对下，每个右端点只能关闭当前最内层左端点，故逐步操作唯一。固定规则
若唯一产生良构配对，同一论证随即适用。若既删除配对又不指定规则，同一串可支持不同括号；
例如 `αβγβαγ` 可候选地包含 `βγβ` 或 `γβαγ`，标签本身不决定哪个区间承担返回关系。
最近同标签闭合等确定性规则可以排除这种歧义，但唯一性只相对于所选规则成立，该规则不是
裸标签串的典范结构。证毕。

### 3.4 并发历史

并发历史写作有限偏序：

```text
H_parallel=(V,≺,λ).
```

`≺` 保存已知的先后关系。拓扑排序只是线性扩展，不是偏序本体。

**命题 6（拓扑排序不产生唯一因果顺序）。** 若两个事件在偏序中不可比，则至少存在两个
保持原偏序的线性扩展，分别采用相反的二者次序。

**证明。** 对不可比元素分别加入 `a≺b` 与 `b≺a` 均不成环，两个扩展都能继续拓扑排序。
证毕。

单栈只适用于已经全序化且良构嵌套的链。并发分支的身份、分支内顺序、跨分支关系与汇合
条件必须额外保留；不能通过任意线性化把它们伪装成一个 64 形式。

## 4. 四维以上的保真边界

### 4.1 不自动同构

保留签名记为：

```text
Σ_keep=(F_keep,R_keep,O_keep),
```

分别列出必须保留的运算、关系和观察。两个表示相对于该签名同构，是指存在双射及其逆，
且二者都保持签名中的全部结构。

**命题 7（布尔闭包维数可辨）。** 若 `n≠4`，带包含、交、并、补、空元和全集的 `B_n`
不与 `B_4` 同构。

**证明。** `|B_n|=2^n`，只有 `n=4` 时等于 16；最大链长度 `n+1` 也随维数变化。证毕。

动态侧同样如此。五维互异词的长度向量为：

```text
(5,20,60,120,120),     |W_5|=325.
```

一般地，保留词长时，一字词层已有 `n` 与 4 的基数差异。因此 `n≠4` 时不能与 `W_4`
同构。325 仍只是互异有限词，不是全部实际历史。

### 4.2 条件商

等价关系 `~` 是相对于 `Σ_keep` 的同余，须保证：保留运算对等价代表元相容；保留关系的
真值不因换代表元改变；保留观察在同类元素上取相同值。设 `X` 为任意集合，条件商约减
包括：

```text
(Σ_keep,~,q,h,loss),
q:X→X/~,     h:X/~→Y.
```

只有当 `h` 是相对于签名的同构时，才能说商 `X/~` 与 `Y` 同构；除非每类为单点，不能
说 `X` 与 `Y` 同构。`loss` 必须列出被合并的差异。

令 `E_5=E_4∪{e_5}`，并定义：

```text
I ~_forget J  ⇔  I∩E_4=J∩E_4.
```

在交、并、相对 `E_4` 的补、空元和全集的签名下，映射 `[I]↦I∩E_4` 给出
`B_5/~_forget ≅ B_4`。它明确丢失一比特：原集合是否含 `e_5`。

反之，按子集基数定义 `I~_size J` 不能保持交。取 `I={e_1}`、`J={e_2}`、
`K={e_1}`，虽 `I~_size J`，但 `I∩K` 与 `J∩K` 的基数不同。因此商运算不良定义。

### 4.3 递归表示

递归表示必须给出：

```text
decompose:X→(local_units,connectors),
rebuild:(local_units,connectors)→X,
loss.
```

只有双向复合为恒等、保持声明签名且 `loss` 为空时，才是相应范围内的无损表示。

**定理（布尔闭包的乘积分解；来源定理 2）。** 若有限集合 `E_m` 与 `F_n` 不交，则：

```text
P(E_m∪F_n) ≅ P(E_m)×P(F_n),
```

并保持包含、交、并、补、空元和全集。

**证明。** 取 `φ(I)=(I∩E_m,I∩F_n)` 与 `ψ(U,V)=U∪V`。不交性保证二者互逆，集合运算
逐分量保持。证毕。

写 `n=4q+r`，`0≤r<4`。选定一个把 `E_n` 分成 `q` 个带身份的四元素块和一个 `r` 元
余块的分区后，反复应用定理可得：

```text
B_n ≅ (B_4)^q × B_r.
```

这一同构依赖所选分区，并非典范同构；所得对象是一组带分量身份和余块的乘积，不是单个
`B_4`。

动态词还需连接信息。将字母表分为不交的 `D_A、D_B`，并令
`W_m^ε=W_m∪{ε}`、`W_n^ε=W_n∪{ε}`。一个全局非空词由两个保持内部顺序的局部投影词
以及交错记录 `σ` 唯一决定；某个局部投影可以是空词，`σ` 标明每个全局位置来自哪一
字母表。没有 `σ`，同一非空局部词对不能区分 `a_1b_1` 与 `b_1a_1`，所以一般有：

```text
W_(m+n) ≠ (W_m^ε×W_n^ε)\{(ε,ε)}.
```

### 4.4 六种不同的保真主张

| 名称 | 必须提供 | 可以损失 |
| --- | --- | --- |
| 同构 | 双向互逆映射及全部保留结构证明 | 不可损失声明结构 |
| 条件商 | 同余、商映射、目标同构与损失表 | 类内差异及未保留结构 |
| 投影 | 源、目标、映射与观察用途 | 可以 |
| 压缩 | 编码、解码条件与误差边界 | 由无损或有损声明决定 |
| 局部四分 | 四部分、范围、覆盖、重叠与连接 | 未经证明均未知 |
| 递归表示 | 前向分解、局部单元、连接、重建与损失 | 由复合与保持证明决定 |

名称不改变函数是否单射、满射、可逆，也不使运算自动穿过等价类。把投影、压缩或目录命名
为“同构”，不能补足缺失证明。

## 5. 结论与解释边界

本文建立的结果是：

1. 静态函数基与动态关系基类型不同，裸结构独立置换且没有典范配对；
2. `B_1…B_4` 的总数是 `2,4,8,16`，`W_1…W_4` 的总数是 `1,4,15,64`；
3. `R_4` 的首个满足维数是 4；这是由完整布尔闭包与 `R_4` 的定义直接得到的登记结果；
4. 64 是保根、保序、方向互异的有限形式数，不是全部历史数；
5. 栈解析的唯一性依赖已给出的良构配对或固定匹配规则，并发偏序不能由任一线性化替代；
6. 更高维不会自动与四维同构；约减必须区分商、投影、压缩和递归表示；
7. 静态布尔结构可相对于选定分区按四元素局部块与余块无损乘积分解，动态结构还必须
   保留交错连接。

本文没有证明任何领域必然具有这两组四维基，没有给四个静态元素或四个动态方向命名，
没有把静态三元子集解释为转换面，也没有规定一次实际工作应怎样开始、执行或结束。把这些
结构用于一个现实对象时，必须另行给出解释映射、来源、分离与覆盖见证、实际历史和主张
上限。那些应用义务由独立的通用工程文章承担，不反向改变本文定义。
